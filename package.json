{
  "name": "domodule",
  "version": "4.0.1",
  "description": "Class based modules",
  "main": "dist/domodule.js",
  "keywords": [
    "module",
    "browser"
  ],
  "scripts": {
    "dev": "nswatch",
    "build": "mkdir -p dist && browserify lib/domodule.js -t [ babelify --presets [ es2015 ] ] -o dist/domodule.js -s Domodule",
    "test": "browserify -t [ babelify --presets [ es2015 ] ] test/domodule.test.js | tape-run --browser phantomjs --render tap-spec",
    "lint": "eslint . --ignore-pattern dist",
    "prepublish": "npm run build"
  },
  "watch": {
    "lib/*.js": [
      "lint",
      "test"
    ],
    "test/*.js": [
      "lint",
      "test"
    ]
  },
  "repository": "https://github.com/firstandthird/domodule",
  "author": "First+Third",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1",
    "concurrently": "^3.1.0",
    "eslint": "^3.5.0",
    "eslint-config-firstandthird": "^3.0.2",
    "eslint-plugin-import": "^2.2.0",
    "nswatch": "^0.2.0",
    "phantomjs": "^2.1.7",
    "static-server": "^2.0.3",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.2",
    "tape-run": "^2.1.5"
  },
  "eslintConfig": {
    "env": {
      "browser": true
    },
    "extends": "firstandthird"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  },
  "dependencies": {
    "attrobj": "1.1.1"
  }
}
